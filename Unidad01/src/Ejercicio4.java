/*
 * Autor: Jordi Santos Torres
 * versión 1.0.0
 */
public class Ejercicio4 {
	public static void main(String[] args) {
		int x = 4;
		int y = 5;
		int suma = x + y;
		int resta = y - x;
		int producto = x * y;
		float div = y / x;
		
		
		System.out.println("Suma:" + suma + " Resta:" + resta + " Mult:" + producto + " Division:" + div);//Muestra por pantalla el resultado 
 	}
}
